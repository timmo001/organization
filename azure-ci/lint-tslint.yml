parameters:
  path: "$(System.DefaultWorkingDirectory)"

jobs:
  - job: "TSLint"
    pool:
      vmImage: "ubuntu-latest"
    steps:
      - script: npm install --global tslint typescript
        displayName: "Install"
      - script: |
          touch ${{ parameters.path }}/dummy.ts
          tslint -c ${{ parameters.path }}/tsconfig.json '${{ parameters.path }}/**/*.ts*'
        displayName: "Install and Run"
